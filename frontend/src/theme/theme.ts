import { createTheme } from '@mui/system';

export const theme = createTheme({
  palette: {
    main: {
      white: '#FFFFFF',
      black: '#000000',
    },
    primary: {
      pageBackground: '#262E38',
      cardBackground: '#2B3D49',
    },
  },
});
