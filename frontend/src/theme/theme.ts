import { createTheme } from '@mui/material/styles';

export const theme = createTheme({
  palette: {
    primary: {
      main: '#FFE082',
    },
    secondary: {
      main: '#2B3D49',
    },
    background: {
      default: '#262E38',
      paper: '#3C4C57',
    },
    text: {
      primary: '#FFFFFF',
    },
  },
});
