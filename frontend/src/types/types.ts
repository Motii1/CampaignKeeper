export enum CustomButtonBehavior {
  Submit,
  Upload,
  Func,
}

export enum CustomButtonType {
  Accent,
  Primary,
  Delete,
}

export enum FabIcon {
  Add,
  Save,
  Delete,
}

export enum StartViewDialog {
  New,
  Edit,
}
